[project]
name = "biorce-mini"
version = "0.1.0"
description = "A RAG-based biomedical assistant"
requires-python = ">=3.10"
dependencies = [
    "google-genai",
    "python-dotenv",
    "streamlit",
    "langgraph",
    "chromadb",
    "sentence-transformers",
    "pydantic",
    "langfuse>=2.0",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff>=0.15.6",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
