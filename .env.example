GEMINI_API_KEY="PASTE_YOUR_KEY"

# Langfuse — get keys from https://cloud.langfuse.com (free Hobby plan)
# Leave empty to disable tracing
LANGFUSE_PUBLIC_KEY="pk-lf-..."
LANGFUSE_SECRET_KEY="sk-lf-..."
LANGFUSE_HOST="https://cloud.langfuse.com"

# HuggingFace Hub Token
# Optional: enables higher rate limits and faster model downloads
# Without this, you'll get a warning but everything still works (degraded performance)
HF_TOKEN="hf_..."

